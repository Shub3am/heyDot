[package]
name = "dot-screen"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
publish = false

[dependencies]
image = { version = "0.25.10", default-features = false, features = ["jpeg"] }
thiserror = "2.0.20"
