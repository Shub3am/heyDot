[package]
name = "dot-models"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
publish = false

[dependencies]
sysinfo = { version = "0.39.6", default-features = false, features = ["system"] }
